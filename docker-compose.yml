version: "3.9"

services:
  ml-service:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: credit-default-api
    ports:
      - "5000:5000"
    environment:
      - MODEL_PATH=/app/models/credit_default_lr.joblib
    restart: unless-stopped
